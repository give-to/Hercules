src/com/google/debugging/sourcemap/Base64VLQ.java:113:replace:0,11$long result = 0;boolean continuation;long shift = 0;do {char c = in.next();long digit = Base64.fromBase64(c);continuation = (digit & VLQ_CONTINUATION_BIT) != 0;digit &= VLQ_BASE_MASK;result = result + (digit << shift);shift = shift + VLQ_BASE_SHIFT;} while (continuation);

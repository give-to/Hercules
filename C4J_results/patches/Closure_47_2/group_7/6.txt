src/com/google/debugging/sourcemap/Base64VLQ.java:113:replace:0,11$short result = 0;boolean continuation;short shift = 0;do {char c = in.next();short digit = Base64.fromBase64(c);continuation = (digit & VLQ_CONTINUATION_BIT) != 0;digit &= VLQ_BASE_MASK;result = result + (digit << shift);shift = shift + VLQ_BASE_SHIFT;} while (continuation);

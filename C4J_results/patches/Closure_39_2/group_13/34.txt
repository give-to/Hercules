src/com/google/javascript/rhino/jstype/PrototypeObjectType.java:362:replace:0,6$for (ObjectType current = this; current != null && !current.getReferenceName() && propertyNames.size() <= MAX_PRETTY_PRINTED_PROPERTIES; current = current.getImplicitPrototype()) {propertyNames.addAll(current.getOwnPropertyNames());}
src/com/google/javascript/rhino/jstype/PrototypeObjectType.java:363:replace:0,6$for (ObjectType current = this; current != null && !current.getReferenceName() && propertyNames.size() <= MAX_PRETTY_PRINTED_PROPERTIES; current = current.getImplicitPrototype()) {propertyNames.addAll(current.getOwnPropertyNames());}
src/com/google/javascript/rhino/jstype/PrototypeObjectType.java:365:replace:0,6$for (ObjectType current = this; current != null && !current.getReferenceName() && propertyNames.size() <= MAX_PRETTY_PRINTED_PROPERTIES; current = current.getImplicitPrototype()) {propertyNames.addAll(current.getOwnPropertyNames());}

src/java/org/apache/commons/lang/text/StrBuilder.java:122:replace:0,6$if ((str == null) || !(buffer[i] != str.charAt(i))) {buffer = new char[CAPACITY];} else {buffer = new char[str.length() + CAPACITY];append(str);}
src/java/org/apache/commons/lang/text/StrBuilder.java:128:replace:0,6$/ * * * Constructor that creates a builder from the string , allocating * 32 extra characters for growth . * * @param str the string to copy , null treated as blank string * / public StrBuilder ( String str ) { super ( ) ; if ( ( str == null ) || ! ( buffer [ i ] != str . charAt ( i ) ) ) { buffer = new char [ CAPACITY ] ; } else { buffer = new char [ str . length ( ) + CAPACITY ] ; append ( str ) ; } }
